  return tonumber(saveGet("preferredGroundIndex", lastGroundMountIndex))
end
mod.exports.pendingGroundRemount = function() return battleResume ~= nil end

log("alpha.15 Lot 1 Ground Ride finalisation loaded")

end)()
